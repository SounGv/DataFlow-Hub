# Netlify — DATAFLOW HUB frontend
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback (react-router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
